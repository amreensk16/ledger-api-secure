# Baseline zero-trust posture: nothing in the payments namespace can talk to
# anything else unless a more specific NetworkPolicy explicitly allows it.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: payments
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
